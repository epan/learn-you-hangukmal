import React from 'react';

const PhraseForm = () => (
  <div>
    Add a phrase:
    <input id="english-phrase" placeholder="english"></input> || <input id="korean-phrase" placeholder="korean"></input> <button id="submit-phrase">Add a new phrase</button>
  </div>
);

export default PhraseForm;
